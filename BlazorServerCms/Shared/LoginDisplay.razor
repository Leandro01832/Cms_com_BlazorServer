@inject IJSRuntime js
@inject NavigationManager navigation
@inject RepositoryPagina repositoryPagina

@code{

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {      
      
    }

    protected override async Task OnInitializedAsync()
    {       
         @*
        var resultado = await Context.SubFiltro
        .Include(f => f.Pagina)
        .Where(x => x.UltimaPasta) 
        .OrderBy(sub => sub.CamadaId)
        .ToListAsync();
        var c = Context.Content
        .FirstOrDefault(c => c.GetType() == typeof(Comment));        
        
        for(var h = 1; h <= resultado.Count; h++)
        {
            var fil = await Context.SubFiltro
            .AsNoTracking()
            .Include(f => f.Pagina)
            .FirstAsync(s => s.Id == resultado[h - 1].FiltroId);
            if(fil.Pagina.Count > 0)            
            resultado[h - 1].IncluiPagina(c);
        }       
        await Context.SaveChangesAsync();
    

     var contents = await Context.Content
    .Where(c => c.StoryId==2)
    .OrderBy(c => c.Id)
    .Skip(50).ToListAsync();

    for(var i = 0; i < 178; i++)
    {
        var criterio = new Criterio
        {
            Id = contents[i].Id,
             CamadaId = 9,
              Ativo = true,
               DataCriacao = DateTime.UtcNow,
                Descricao = "Descrição",
                LivroId= 1                 
        };
        Context.Add(criterio);
    } *@

    @* 
             
              var fils = Context.SubFiltro
            .Where(f => f.CriterioId == 40)
            .ToList();

            var cri = Context.Criterio
             .Where(c => c.Id > 40)
             .ToList();

             for(int i = 0; i < fils.Count; i++)
             {
                fils[i].CriterioId = cri[i].Id;
                Context.SubFiltro.Update(fils[i]);
             }
                Context.SaveChanges(); *@
                  
                

                // Usando LINQ para encontrar quem não é pai de ninguém

    @* 
var gruposFinais = await Context.SubFiltro
    .Where(pai => !Context.SubFiltro.Any(filho => filho.FiltroId == pai.Id))
    .ToListAsync();

    foreach(var item in gruposFinais)
    {
        if(!item.UltimaPasta)
        {
            var f = new SubFiltro
            {
                FiltroId = item.Id,
                Nome = "ultima pasta",
                CriterioId = 40,
                LivroId = item.LivroId,
                StoryId = item.StoryId,
                UltimaPasta = true,
                CamadaId = item.CamadaId + 1
            };
            await Context.SubFiltro.AddAsync(f);

        }
            
    }      
      
        await Context.SaveChangesAsync(); 
        *@

    

 //   GerarInteligenciaDosFiltros();
    }
 
 }